<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Color de fondo -->
    <solid android:color="@color/orange"/> <!-- Reemplaza con el color deseado -->

    <!-- Bordes redondeados -->
    <corners android:radius="25dp"/> <!-- Radio de las esquinas -->

    <!-- Opcional: Borde (si quieres un borde alrededor del botón) -->
    <stroke
        android:width="22dp"
        android:color="@color/orange"/> <!-- Color del borde -->

    <!-- Opcional: Padding interno -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp"/>
</shape>
